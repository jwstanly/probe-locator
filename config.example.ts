import { Config } from './types';

const config: Config = {
  wifiInterface: 'en0',
  latRange: [30.5, 31.5],
  longRange: [80.2, 81.2],
  city: 'Atlanta',
  encodedToken: 'TOKEN_HERE',
};

export default config;
